#!/usr/bin/env bash
set -euo pipefail

echo "grobot core binary (darwin-arm64) is not bundled in source checkout." >&2
echo "publish pipeline should replace this stub with compiled artifact." >&2
exit 1
